#include "main.h"
#include <stdio.h>
/**
 * main - program that prints its name
 * @rgc: parameter
 * @argv: string to be renamed
 * Return: 0 always 
 */
int main(int argc, char *s)
{
	printf("%s\n", argv[0]);
	return (0);
}
